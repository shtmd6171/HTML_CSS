<!DOCTYPE html>
<html lang="ko">

<head>
	<meta charset="UTF-8">
	<title>jQuery Element Selection</title>
	<script src="https://code.jquery.com/jquery-1.12.4.min.js"></script>
	<script>
		//일반적인 Getter Setter의 역할을 하는 문법
		// .html()를 통해 param을 반환하거나 설정함
		// h1 요소의 값을  id가 text인 요소의 값으로 설정하는 것
		$(function() {
			$("#btn1").on("click", function() {
				// 메소드에 인수를 전달하지 않고 호출하면, 해당 HTML 요소에서 값을 읽어오는 getter
				var newText = $("h1").html();
				//인수를 전달하고 호출하면, 해당 HTML 요소에 새로운 값을 설정하는 setter
				$("#text").html(newText);
			});
			$("#btn2").on("click", function() {
				// method chaining
				//여러 개의 메소드가 연속으로 호출되는 것
				// .find(), .eq(), .html()를 연속 호출하는 내용
				// .find() : ()에 속한 모든 자식tag들을 가져옴
				// + ) children() : ()에 속한 바로 아래 자식tag들을 가져옴
				// .eq(n) : 선택한 요소 중 n번째 요소를 선택함
				$("#list").find("li").eq(1).html("두번째 아이템");
				$("#list").find("li").eq(1).html("두번째 아이템")
					//.end() : 바로 이전 선택했던 요소의 집합을 다시 선택한다
					// 다시 id가 list 인 요소의 자손 요소 중에서 <li>요소를 모두 선택
					.end()
					.eq(2).html("세번째 아이템");
			});
			// 대표적 GetSetter 메소드인 width, height를 사용했다
			// width,height()를 통해 넓이 높이를 반환하거나 설정함
			// box의 width와 height 값을 불러온다
			// box의 width와 height를 버튼을 누를 때마다 1/2씩 줄어들게 했다
			// 더욱 자세한 GetSetter 메소드는 00_CDN.html의 3번 주석에서 확인할 수 있다.
			$("#getter").on("click", function(){
				var size = "너비 : " + $("#box").width() + " 높이 : "+$("#box").height();
				$("#text2").html(size);
			});
			$("#setter").on("click", function(){
				var w = $("#box").width();
				var h = $("#box").height();
				// method chaining
				$("#box").width(w/2).height(h/2);
				var size = "너비 : " + $("#box").width() + " 높이 : "+$("#box").height();
				$("#text2").html(size);
			});

		});
	</script>
</head>

<body>
	<h1>Getter Setter 메소드 설정</h1>
	<button id="btn1">새 텍스트 생성</button>
	<p id="text"></p>

	<h1>메소드 체이닝</h1>
	<ul id="list">
		<li>첫 번째 아이템이에요!</li>
		<li>두 번째 아이템이에요!</li>
		<li>세 번째 아이템이에요!</li>
	</ul>
	<button id="btn2">텍스트 변경</button>

	<h1>.width() 메소드와 .height() 메소드</h1>
	<p>아래의 버튼을 누르면 다음 div 요소의 크기를 읽어오거나 설정할 수 있어요!!</p>
	<button id="getter">크기 읽어오기</button>
	<button id="setter">크기 줄이기</button><br><br>
	<div id="box" style="width: 400px; height: 200px; background-color: yellow;"></div>
	<p id="text2"></p>


</body>

</html>
